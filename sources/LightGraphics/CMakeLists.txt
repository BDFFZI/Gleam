addModule()

target_link_libraries("${ModuleName}" PUBLIC LightGL)

target_link_libraries("${ModuleName}" PUBLIC LightMath)

target_link_libraries("${ModuleName}" PUBLIC LightUtility)

target_link_libraries("${ModuleName}" PUBLIC LightImport)